---

- hosts: all
  become: true
  pre_tasks:

  - name: update repo index
    package:
      update_cache: yes

- hosts: web_servers
  become: true
  tasks:

  - name: install apache package
    package:
      name: "{{ apache_package }}"
      state: latest
